#include <stdio.h>
#define pi 3.14

int main(void)
{
    //radius
    int radius = 10;
    float volume = (4.0f/3.0f) * pi * radius * radius * radius;
    printf("The volume of sphere is %.2f\n", volume);

return 0;

}